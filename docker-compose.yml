services:
  tokenizer:
    build: .
    container_name: tokenizer_dev
    volumes:
      - .:/app
    working_dir: /app
    ports:
      - "8545:8545"
    stdin_open: true
    tty: true
    env_file:
      - .env
    environment:
      - PATH=/root/.foundry/bin:$PATH
    command: ["bash"]